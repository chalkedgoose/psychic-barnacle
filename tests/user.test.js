const chai = require('chai');
const chaiHTTP = require('chai-http');
const should = chai.should();

chai.use(chaiHTTP);

/**
 * Tests to be implemented before production use 
 * with Travis CI/CD or Circle CI/CD
 */